pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "Starting ROS environment..."'
                sh 'echo ${WORKSPACE}'

            }
        }
    }
}
