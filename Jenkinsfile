pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Starting ROS environment..."'
                sh 'echo ${env.WORKSPACE}'

            }
        }
    }
}
